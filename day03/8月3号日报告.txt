2020年8月3号
	
	早上7点进班背面试题，先把之前背会的回顾一遍，再开始背后面的内容，今天第一节课老师也要求大家背面试题，
我背了半节课面试题就开始抄写今天需要抄写的面试题内容。

	第二节课和第三节课开始上正课，今天上午学的是ssm整合vue---页面静态化展示静态信息，先在eclipse里创建项目，
来准备进行vue整合练习，首先通过html页面制作静态化展示信息，需要循环遍历显示数据运用了v-for="(user,index) in users"，
然后进行添加功能，由于是静态化不会跳转所以需要运用到v-if="show"来进行添加与展示界面的转换，如果v-if="!show"那么show
就是false点击添加按钮就会显示到添加功能，提交后show就是true则会转回现实界面，通过给show赋值true或false使界面更加整洁，
修改功能和添加类似不过需要在按钮里添加一个v-if=!isEdit，然后使用钩子函数进行前台分页操作，跟以前学过的分页操作比较相似，
然后再制作页码，做完前台展示相关的就开始做根据姓名模糊查询，运用了indexOf()方法，作用和contains作用相似，返回值如果是-1则代表有值，
返回值如果是0，则代表没值。

	下午第一节课老师讲了关于maven的安装与配置环境变量，演示了maven在eclipse中如何配置，第一节课下课后银川教育局来检查，
老师就让我们回宿舍了，下午在宿舍里做了老师发的代码作业，也会出现点小错误，但是仔细找了找错误，还是把代码作业完成了。

	晚自习来到班里，先把上午没写完的面试题抄写做完，然后开始帮助同学找代码中的错误，最后再回顾一遍今天所学习的内容，用每日输出的方式
做了出来，把今天的作业都整理完后就可以用git提交作业了。

	总结：今天学的东西虽然不多但是也都是最基础的东西，还是需要好好掌握。
	
	