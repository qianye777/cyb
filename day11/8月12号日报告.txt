2020年8月12号
	
	今天早上7点四十到班里，然后开始了日常进行的早读。开始背面试题，背到第一节课下课。
	
	第二节课开始上正课，今天学的是ssm里的echarts报表，学的有饼图、柱状图和折线图。先把jsp页面里的删除弹框复制，
然后把删除按钮和点击删除都删掉，然后写一个div 给他一个id main 再给他高和宽，然后把该改的del改成echarts。然后再把删除按钮复制，
更改id为btn_echarts只流血model和show两行，其他内容都删掉，然后再改成上面改的echarts路径，再写一个myecharts，将上面的main
用myecharts根据id获取过来赋值给myecharts。再把url更改，再写个console.log(res)定义一个x轴对象xdata和y轴对象ydata，给他们新建一个
数组，用res.list.forEach(function(res){}将数据库的数据推给xdata和ydata,这里的list是从后台写的代码里的list获取的，然后复制echarts的js文件,
再jsp页面里调用一下，把三个图的代码复制粘贴进来进行修改，把获取值的地方改成xdata和ydata。后台里面把删除方法复制粘贴，更改
成echarts，只在try里面留一个success，创建一个fdbEcharts的方法返回list这样就可以把list的值传给前台了，再在service里面创建findEcharts
方法，再导入mapper里，最后把xml里面的sql语句通过提前在数据库书写能获取值的正确代码给复制粘贴过来，再运行就可以实现echarts报表了。
	剩下的自习课就开始练今天所学习的内容了，我把echarts敲了一遍后没有实现，然后根据老师发的录屏看了一遍找了找错，就成功
将echarts报表功能实现了。

	今天下午老师没有讲课，因为今天的项目都学完了。就让我们进行自主学习，我的tomcat出了问题，就找别的版本安装，也没有好，
最后找了个7.0版本的将就着用了。勉强可以用，然后剩下的时间打了打导入和导出的功能，打了一遍增删改查和导入导出后也就下课了。

	晚自习的时候把今天老师布置的代码作业给做完了，echarts图只有个柱状图还是很好写的，剩下的功能就是增删改查，写了没多久，
就写完了，然后剩下的时间背了背理论，就开始整理今天的其他作业。把作业都整理完成就可以上传到git里面了。

	
	
	
	
	
	
	
	
	